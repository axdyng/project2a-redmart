<!-- Static Search Bar -->
<div class="search-bar">
  <div class="input-group search-bar">
    <input type="search" class="form-control" placeholder="Search for a product...">

    <span class="input-group-btn">
      <button class="btn btn-default" type="button">
        <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
      </button>
    </span>
  </div>
</div>




<%= form_tag products_path, method: 'get' do %>
  <!-- Form fields -->
  <div class="field">
    <%= text_field_tag :search, params[:search], placeholder: 'Search for product...' %>
  </div>

  <div class="actions">
    <%= submit_tag "Search", class: "btn btn-primary" %>
  </div>
<% end %>

<!-- example of syntax test on button + icon -->
<span class="input-group-btn">
  <div class="actions">
    <%= submit_tag "Search", class: "btn btn-primary" do %>
    <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
    <% end %>
  </div>
</span>
